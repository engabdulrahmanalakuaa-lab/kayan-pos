name: Build Kayan POS to EXE
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # تثبيت المكتبات وتمرير متغيرات البيئة لـ node-gyp في نفس الخطوة
      - name: Install Dependencies
        run: npm install
        env:
          npm_config_msvs_version: "2022"

      # بناء التطبيق وتمرير المتغيرات لضمان عمل إعادة البناء بنجاح
      - name: Build Electron App to EXE
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm_config_msvs_version: "2022"

      - name: Upload EXE Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Kayan-Bukhari-POS-Installer
          path: dist/*.exe
